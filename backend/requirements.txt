fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pydantic[email]==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pandas==2.2.3
openpyxl==3.1.5
python-dotenv==1.0.1
httpx==0.27.2
pytest==8.3.3
# Slice 3 — Anthropic SDK for NL → structured-rule translation.
anthropic>=0.40.0
# Slice 1 — Fernet credential sealing.
cryptography>=42.0.0
# P5 — live Oracle EBS scanner. Thin-mode driver, no Instant Client
# install needed. Soft-imported in app/discovery/live_ebs.py — the
# workbench boots fine without it; missing driver → live EBS probes
# return a `degraded` outcome rather than 500.
oracledb>=2.4.0
